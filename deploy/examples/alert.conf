# Copy to ~/.config/dotclaude/alert.conf — read by failure-alert.sh.
# Without this file (or with empty values) failure alerts are a silent no-op.
ALERT_EMAIL=you@example.com
SMTP_RELAY=smtp.internal.example.com
